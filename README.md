# synarius-core

GUI-loses Simulationsframework.

## Goals

- Provide a minimal, testable simulation core (step-based).
- Keep GUI concerns in `synarius-studio`.

## Run (module)

```bash
python -m synarius_core
```

